// Package controller implements the Kubernetes controllers for the
// kube-workspaces project. It contains reconcilers for Workspace, User,
// and AuthConfig custom resources, managing the lifecycle of workspace
// StatefulSets, user namespaces, RBAC bindings, and authentication
// configuration.
package controller
